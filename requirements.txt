# Dependencies for all projects in this repo (Python 3.14)
# Pinned to the versions used during development.
# Install with:  pip install -r requirements.txt

# --- Core numerical / JAX stack (all projects) ---
jax==0.11.0
jaxlib==0.11.0
numpy==2.4.4

# --- Neural Network (Flax NNX version) ---
flax==0.12.7
optax==0.2.8

# --- Nearest Neighbor ---
scikit-learn==1.9.0
xgboost==3.3.0
faiss-cpu==1.14.3

# --- Linear Regression ---
pandas          # unpinned: not installed in the dev env at pin time

# --- Plotting / IO (shared) ---
matplotlib==3.11.0
Pillow==12.3.0
